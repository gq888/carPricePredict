# 车辆价格预测模型优化计划书

## 一、优化目标
1. 提高模型的预测性能
2. 增强模型的泛化能力和稳定性
3. 提升模型的可解释性

## 二、优化方向与具体步骤

### 1. 数据预处理优化（优先级：★★★★★）

#### 1.1 缺失值处理优化
**任务描述**：针对不同类型特征采用差异化的缺失值填充策略
**执行步骤**：
- 分析各特征的缺失情况：`train.isnull().sum()`
- 对于离散型特征（gearbox, notRepaired）：使用众数填充
- 对于连续型特征（power, km等）：使用中位数填充（减少异常值影响）
- 实现代码：
  ```python
  from sklearn.impute import SimpleImputer
  
  # 分离离散型和连续型特征
  categorical_cols = ['gearbox', 'notRepaired']
  continuous_cols = [col for col in feature_cols if col not in categorical_cols]
  
  # 离散型特征使用众数填充
  cat_imputer = SimpleImputer(strategy='most_frequent')
  X_train_cat = cat_imputer.fit_transform(X_train[categorical_cols])
  X_test_cat = cat_imputer.transform(X_test[categorical_cols])
  
  # 连续型特征使用中位数填充
  cont_imputer = SimpleImputer(strategy='median')
  X_train_cont = cont_imputer.fit_transform(X_train[continuous_cols])
  X_test_cont = cont_imputer.transform(X_test[continuous_cols])
  
  # 合并特征
  X_train = np.hstack((X_train_cont, X_train_cat))
  X_test = np.hstack((X_test_cont, X_test_cat))
  ```

#### 1.2 异常值处理
**任务描述**：识别并处理极端异常值
**执行步骤**：
- 可视化分析power和km特征的分布：`plt.boxplot(train['power'])`
- 对power特征：使用99.9%分位数截断（去除极端高功率值）
- 对km特征：处理0值和极端高里程值（基于业务规则）
- 实现代码：
  ```python
  # 处理power异常值
  power_upper = train['power'].quantile(0.999)
  train.loc[train['power'] > power_upper, 'power'] = power_upper
  test.loc[test['power'] > power_upper, 'power'] = power_upper
  
  # 处理km异常值
  train.loc[train['km'] == 0, 'km'] = train['km'].median()  # 替换0值
  km_upper = train['km'].quantile(0.999)
  train.loc[train['km'] > km_upper, 'km'] = km_upper
  test.loc[test['km'] > km_upper, 'km'] = km_upper
  ```

### 2. 特征工程优化（优先级：★★★★★）

#### 2.1 时间特征提取
**任务描述**：从日期字段中提取有价值的时间特征
**执行步骤**：
- 将regDate和creatDate转换为datetime格式
- 计算车辆使用年限：creatDate - regDate
- 提取注册月份、季度等周期特征
- 实现代码：
  ```python
  # 转换日期格式
  train['regDate'] = pd.to_datetime(train['regDate'], format='%Y%m%d', errors='coerce')
  train['creatDate'] = pd.to_datetime(train['creatDate'], format='%Y%m%d', errors='coerce')
  test['regDate'] = pd.to_datetime(test['regDate'], format='%Y%m%d', errors='coerce')
  test['creatDate'] = pd.to_datetime(test['creatDate'], format='%Y%m%d', errors='coerce')
  
  # 计算使用年限（天）
  train['car_age_days'] = (train['creatDate'] - train['regDate']).dt.days
  test['car_age_days'] = (test['creatDate'] - test['regDate']).dt.days
  
  # 提取注册月份和季度
  train['reg_month'] = train['regDate'].dt.month
  train['reg_quarter'] = train['regDate'].dt.quarter
  test['reg_month'] = test['regDate'].dt.month
  test['reg_quarter'] = test['regDate'].dt.quarter
  ```

#### 2.2 类别特征编码
**任务描述**：重新纳入并编码Type相关特征
**执行步骤**：
- 将BType和FType特征重新纳入特征列表
- 使用One-Hot编码处理低基数类别特征
- 实现代码：
  ```python
  # 重新定义特征列，包含Type特征
  feature_cols = [
      col for col in numerical_cols if col not in [
          'ID', 'name', 'regDate', 'creatDate', 'price', 'model', 'brand',
          'regionCode', 'seller'
      ]
  ]
  # 添加Type特征
  feature_cols.extend(['BType', 'FType'])
  
  # 进行One-Hot编码
  X_train = pd.get_dummies(train[feature_cols], columns=['BType', 'FType'])
  X_test = pd.get_dummies(test[feature_cols], columns=['BType', 'FType'])
  
  # 确保测试集和训练集有相同的列
  X_test = X_test.reindex(columns=X_train.columns, fill_value=0)
  ```

#### 2.3 特征组合与选择
**任务描述**：创建有意义的特征组合并筛选重要特征
**执行步骤**：
- 创建功率里程比特征：power/km
- 使用XGBoost的特征重要性筛选前20个重要特征
- 实现代码：
  ```python
  # 创建特征组合
  train['power_km_ratio'] = train['power'] / (train['km'] + 1)  # +1避免除零
  test['power_km_ratio'] = test['power'] / (test['km'] + 1)
  
  # 使用XGBoost进行特征重要性评估
  model = XGBRegressor(objective='reg:squarederror', random_state=42)
  model.fit(X_train, y_train)
  
  # 获取特征重要性
  importances = model.feature_importances_
  feature_importance_df = pd.DataFrame({'feature': X_train.columns, 'importance': importances})
  feature_importance_df = feature_importance_df.sort_values('importance', ascending=False)
  
  # 选择前20个或更多的重要特征
  top_features = feature_importance_df['feature'].head(20).tolist()
  X_train = X_train[top_features]
  X_test = X_test[top_features]
  ```

### 3. 模型调优优化（优先级：★★★★）

#### 3.1 扩展超参数搜索范围
**任务描述**：扩大超参数搜索空间，包含更多重要参数
**执行步骤**：
- 扩展参数网格，包含subsample、colsample_bytree、gamma等参数
- 使用随机搜索代替网格搜索，提高效率
- 实现代码：
  ```python
  from sklearn.model_selection import RandomizedSearchCV
  import scipy.stats as stats
  
  # 定义扩展的参数分布
  param_dist = {
      'n_estimators': stats.randint(100, 300),
      'max_depth': stats.randint(3, 10),
      'learning_rate': stats.uniform(0.01, 0.19),  # 0.01到0.2
      'subsample': stats.uniform(0.6, 0.4),  # 0.6到1.0
      'colsample_bytree': stats.uniform(0.6, 0.4),  # 0.6到1.0
      'gamma': stats.uniform(0, 1),
      'reg_alpha': stats.uniform(0, 1),
      'reg_lambda': stats.uniform(0, 1)
  }
  
  # 使用随机搜索
  random_search = RandomizedSearchCV(
      estimator=XGBRegressor(objective='reg:squarederror', random_state=42),
      param_distributions=param_dist,
      n_iter=50,  # 搜索50个参数组合
      cv=3,
      scoring='r2',
      n_jobs=-1,
      random_state=42
  )
  
  random_search.fit(X_train, y_train)
  ```

#### 3.2 尝试LightGBM模型
**任务描述**：对比测试LightGBM模型的性能
**执行步骤**：
- 安装并导入LightGBM库
- 训练LightGBM模型并评估性能
- 实现代码：
  ```python
  import lightgbm as lgb
  
  # 创建LightGBM模型
  lgb_model = lgb.LGBMRegressor(objective='regression', random_state=42)
  
  # 交叉验证评估
  lgb_cv_scores = cross_val_score(lgb_model, X_train, y_train, cv=5, scoring='r2')
  print("LightGBM交叉验证R2分数:", lgb_cv_scores.mean())
  ```

### 4. 训练策略优化（优先级：★★★）

#### 4.1 实现早停策略
**任务描述**：添加早停机制，避免模型过拟合
**执行步骤**：
- 划分训练集和验证集
- 在模型训练中添加early_stopping_rounds参数
- 实现代码：
  ```python
  from sklearn.model_selection import train_test_split
  
  # 划分训练集和验证集
  X_train_part, X_val, y_train_part, y_val = train_test_split(
      X_train, y_train, test_size=0.2, random_state=42
  )
  
  # 训练模型并使用早停
  model = XGBRegressor(
      objective='reg:squarederror',
      n_estimators=1000,  # 增加迭代次数
      random_state=42,
      **random_search.best_params_  # 使用之前搜索的最佳参数
  )
  
  model.fit(
      X_train_part, y_train_part,
      eval_set=[(X_val, y_val)],
      eval_metric='r2',
      early_stopping_rounds=50,
      verbose=False
  )
  ```

#### 4.2 模型集成
**任务描述**：融合多个模型的预测结果
**执行步骤**：
- 训练多个不同参数的XGBoost模型
- 使用简单平均或加权平均融合预测结果
- 实现代码：
  ```python
  # 训练多个模型
  model1 = XGBRegressor(objective='reg:squarederror', n_estimators=200, max_depth=5, random_state=42)
  model2 = XGBRegressor(objective='reg:squarederror', n_estimators=200, max_depth=7, random_state=43)
  model3 = XGBRegressor(objective='reg:squarederror', n_estimators=300, max_depth=6, random_state=44)
  
  model1.fit(X_train, y_train)
  model2.fit(X_train, y_train)
  model3.fit(X_train, y_train)
  
  # 融合预测结果
  y_pred1 = model1.predict(X_test)
  y_pred2 = model2.predict(X_test)
  y_pred3 = model3.predict(X_test)
  
  y_pred_ensemble = (y_pred1 + y_pred2 + y_pred3) / 3  # 简单平均
  ```

### 5. 评估与分析优化（优先级：★★★）

#### 5.1 误差分析
**任务描述**：深入分析模型预测误差
**执行步骤**：
- 计算预测残差：y_train - y_pred_train
- 分析残差与各特征的关系
- 识别误差较大的样本类型
- 实现代码：
  ```python
  # 计算残差
  train['pred_price'] = model.predict(X_train)
  train['residual'] = train['price'] - train['pred_price']
  train['abs_residual'] = train['residual'].abs()
  
  # 分析残差与重要特征的关系
  plt.figure(figsize=(12, 6))
  plt.scatter(train['power'], train['abs_residual'], alpha=0.5)
  plt.xlabel('功率')
  plt.ylabel('绝对残差')
  plt.title('功率与预测残差的关系')
  plt.show()
  
  # 查看误差最大的样本
  top_error_samples = train.sort_values('abs_residual', ascending=False).head(20)
  print("误差最大的样本：")
  print(top_error_samples[['price', 'pred_price', 'residual']])
  ```

#### 5.2 多指标评估
**任务描述**：使用多个指标全面评估模型
**执行步骤**：
- 计算MAE、MAPE等评估指标
- 分析不同指标下的模型性能
- 实现代码：
  ```python
  from sklearn.metrics import mean_absolute_error, mean_squared_error
  
  # 计算多种评估指标
  y_pred_train = model.predict(X_train)
  r2 = r2_score(y_train, y_pred_train)
  mae = mean_absolute_error(y_train, y_pred_train)
  mse = mean_squared_error(y_train, y_pred_train)
  rmse = np.sqrt(mse)
  mape = np.mean(np.abs((y_train - y_pred_train) / y_train)) * 100
  
  print(f"R2分数: {r2:.4f}")
  print(f"MAE: {mae:.4f}")
  print(f"RMSE: {rmse:.4f}")
  print(f"MAPE: {mape:.2f}%")
  ```

## 三、实施顺序与时间规划

| 阶段 | 优化方向 | 预计时间 |
|------|----------|----------|
| 1    | 数据预处理优化 | 1-2天 |
| 2    | 特征工程优化   | 2-3天 |
| 3    | 模型调优优化   | 2-3天 |
| 4    | 训练策略优化   | 1-2天 |
| 5    | 评估与分析优化 | 1天 |
| 6    | 综合测试与调优 | 2天 |

## 四、预期效果评估

| 优化措施 | 预期效果 | 验证方式 |
|----------|----------|----------|
| 数据预处理优化 | 减少异常值和缺失值对模型的影响 | 对比优化前后的交叉验证分数 |
| 特征工程优化 | 增加模型可利用的信息 | 对比特征重要性和模型性能 |
| 模型调优优化 | 找到更优的模型参数配置 | 对比调优前后的模型性能 |
| 训练策略优化 | 提高模型训练效率和稳定性 | 对比训练时间和模型泛化能力 |
| 评估与分析优化 | 深入了解模型优缺点 | 误差分析报告和多指标评估 |

## 五、风险与应对措施

1. **特征工程过度拟合风险**：
   - 应对措施：使用交叉验证严格评估新特征的有效性，避免添加过多冗余特征

2. **超参数搜索计算资源消耗大**：
   - 应对措施：使用随机搜索代替网格搜索，合理设置搜索次数和参数范围

3. **模型集成效果不明显**：
   - 应对措施：确保集成的模型具有足够的多样性，尝试不同的融合策略

通过以上详细的优化计划，我们可以系统地提升车辆价格预测模型的性能和稳定性，达到预期的优化目标。